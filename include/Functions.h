#ifndef FUNCTIONS_H
#define FUNCTIONS_H

#include <vector>
#include <string>

#include "Components.h"

int find(std::vector<Node>& vec, Node node);
int find(std::vector<Node>& vec, std::string);

#endif