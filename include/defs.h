
#define INF (INT_MAX) // largest possible integer is essentially infinity. 
                      // this means when any node is compared to for the first time
                      // the value being compared will always replace it.